To run this curl demo on ubuntu, you need to install libcurl - the easiest way to do it probably is to run this command:

   sudo apt-get install libcurl4-gnutls-dev

Then you can run this:

   g++ [FILE] -lcurl

And that should give you your a.out file.
